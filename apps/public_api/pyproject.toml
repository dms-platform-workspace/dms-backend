[project]
name = "public-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "foundation",
]

[tool.uv.sources]
foundation = { workspace = true }
