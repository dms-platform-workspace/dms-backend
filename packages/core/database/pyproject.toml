[project]
name = "database"
version = "0.1.0"
description = "Core database infrastructure."
readme = "README.md"
authors = [
    { name = "unxcoder", email = "bqr.ho3ein@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy>=2.0.50",
]

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
